# Framework-owned files in this repository that carry a LOCAL commit.
#
# `.workflow/bin/` and `.github/` are installed by agent-dev-flow and are REPLACED WHOLESALE on
# every refresh. A commit of ours that is the most recent thing to touch one of those files is a
# fix with a half-life of one install (Issue #58). `frameworkpaths_test.go` finds them and fails
# unless the file is declared here with a reason it is not a loss waiting to happen.
#
# THIS FILE IS NOT A SILENCER. An entry is a claim that the local edit has been reconciled with
# upstream — upstreamed and reinstalled, or deliberately accepted as disposable. Adding a line
# without doing that is how the next fix gets lost quietly.
#
# One entry per line:  <commit-sha> <path> <reason>
#
# Blank lines and lines beginning with `#` are ignored.


# ============================================================================================
# EVERY DEBT THIS FILE CARRIED IS DISCHARGED, AND #117 IS WHERE IT HAPPENED.
#
# Eight declarations across seven files, every one of them ending "NOT YET UPSTREAMED — the next
# refresh will delete it". agent-dev-flow a9d81c2 — "carry every downstream fix upstream before it
# is refreshed away", 1,179 insertions over seven files — and 2b94c41 for the archive gate took all
# of them. The #117 refresh then installed them back here.
#
# **THE NUMBER THAT EXPLAINS THE WHOLE THING IS 1,179.** That is how far behind the framework's own
# copies had fallen from the repository running them. Every fail-open fixed here existed only here.
#
# THE PROOF IS NOT THAT UPSTREAM SAYS SO. For five of the seven files the refresh's copy is
# BYTE-IDENTICAL to ours — `cmp` says so, and the installer wrote nothing over them, which is why
# `git diff` for this refresh names five files and not eight. For the two watches the only bytes
# that changed are self-test timing; the #64 tail-reason code and the #89 READY-evidence code are
# byte-identical. And the whole `internal/machinery` suite, which EXECUTES these scripts, is green.
#
# WHY FIVE LINES REMAIN BELOW ANYWAY, and it is a git fact rather than a debt. A declaration is
# needed while OUR commit is the most recent one to touch the file. For the five files the refresh
# returned unchanged, the installer copied identical bytes, so no new commit touches them and our
# commit is still the last one — even though upstream has now spoken for every line in them. The
# precedent is 17c8f9c, recorded below: there a refresh landed OVER the file and the line became
# removable. Here it landed IDENTICALLY, and it did not.
#
# **These five are no longer debts. They are bookkeeping, and the next refresh that changes any of
# these files for any reason retires the line for it.**
# ============================================================================================



24ffb05 .github/workflows/gates.yml Issue #84: exit code 4 needed its own published sentence, or the fact the checker went to the trouble of splitting would reach nobody — which is #52 exactly, where a landed refusal and an absent review shared a description. UPSTREAMED as agent-dev-flow a9d81c2 and RETURNED BYTE-IDENTICAL by the #117 refresh. `reviewgate_test.go`'s `TestPublishDistinguishesTheUnplaceableVerdict` lifts the real publish step out of this file and goes red if rc=4 ever renders as `No current review` again.

d1fd981 .workflow/bin/check-generated.sh Issue #77, carried into #108: the archive gate read the relationship between a change and the specification it generates in ONE direction only — it refused a spec edited WITHOUT an archive and said nothing about a spec regenerated with the archive half done. Eleven occurrences in one day, eleven caught by a person happening to look, zero by a mechanism. UPSTREAMED as agent-dev-flow 2b94c41 and RETURNED BYTE-IDENTICAL by the #117 refresh. THIS ONE IS THE INSTALLER'S REFUSAL PAYING FOR ITSELF: it landed here while the upstream branch was being written, and the new `install.sh` stopped and NAMED this file instead of replacing it, which is how it got carried up rather than deleted. `archivegate_test.go` and five matching `--self-test` arms are what keep it honest.



# ============================================================================================
# THREE LINES WERE REMOVED HERE BECAUSE THE REFRESH LANDED OVER THEIR FILES. Recorded rather than
# silently dropped, because "the line is gone" and "the fix is gone" look identical afterwards:
#
#   36f0f72 .workflow/bin/queue.sh      #79, and #59's debt with it
#   5d0bf82 .workflow/bin/watch-queue.sh #64
#   ec2bcde .workflow/bin/watch-prs.sh   #89
#
# `watch-queue.sh` (#64, the `LOOKUP FAILED` reason cut from the HEAD of an output whose failure is
# its LAST line) and `watch-prs.sh` (#89, `READY` firing on the ABSENCE of negative evidence, at a
# measured 50% false-positive rate) came back with their fix code BYTE-IDENTICAL — the only changed
# bytes in either file are self-test arms that slept a fixed three seconds and now wait for the
# outcome. `watchreason_test.go` and `readyevidence_test.go` pass unchanged.
#
# `queue.sh` IS THE ONE THAT NEEDS READING. #79's queue-side fix and #59's are both gone, and NOT
# because they were dropped: the region they lived in no longer exists. `reviews_waiting` is gone
# with it. The queue no longer asks who authored anything — ownership comes from the branch name,
# the author dispatches one reviewer, and a refusal goes back to THAT reviewer by name. So there is
# no longer a queue for a refusing role to be re-offered the work from, and no author lookup to
# collapse to an empty set.
#
# THE FAIL-OPEN CLASS BOTH FIXES CLOSED IS STILL CLOSED, and it is asserted rather than assumed:
# the `could not determine` half moved intact onto the review-history query, which exits non-zero
# with an `::error::` and never renders as `NO REVIEW HAS HAPPENED`. `authorlookup_test.go` and
# `reviewassignment_test.go` were re-aimed at the new contract and every new assertion was confirmed
# by a mutation that removed the behaviour and turned it red.
#
# ONE PROPERTY WAS TRADED AWAY ON PURPOSE AND IS NOT REPLACED: #59's deliberate REDUNDANCY, two
# independent roles offered the same head so that one unavailable reviewer could not stall the
# board. There is one reviewer per pull request now. **#32 is the outage that redundancy was chosen
# to prevent and it is still open.** The header of `reviewassignment_test.go` carries this at
# length; it is not a solved problem, it is a live trade.
# ============================================================================================


# The healthy state is no entries at all.
#
# There was one: 17c8f9c over `.workflow/bin/check-review.sh`, the #52 fix. It was upstreamed as
# agent-dev-flow aa8c730, and the refresh 1889abd has since landed over that file — so the framework
# has spoken for it and the declaration became unnecessary while this branch was open. The check
# said so, in as many words, and the line was removed. That is the loop working.

# THREE DECLARATIONS REMOVED when the machinery became Python: the files they named are
# shims now, or gone, so the debts they recorded cannot be owed. A declaration that no
# longer applies is the same failure as a memory entry that has stopped being true — it is
# read as current, and it makes the ones that ARE current harder to see.
