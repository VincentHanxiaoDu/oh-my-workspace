# Framework-owned files in this repository that carry a LOCAL commit.
#
# `.workflow/bin/` and `.github/` are installed by agent-dev-flow and are REPLACED WHOLESALE on
# every refresh. A commit of ours that is the most recent thing to touch one of those files is a
# fix with a half-life of one install (Issue #58). `frameworkpaths_test.go` finds them and fails
# unless the file is declared here with a reason it is not a loss waiting to happen.
#
# THIS FILE IS NOT A SILENCER. An entry is a claim that the local edit has been reconciled with
# upstream — upstreamed and reinstalled, or deliberately accepted as disposable. Adding a line
# without doing that is how the next fix gets lost quietly.
#
# One entry per line:  <commit-sha> <path> <reason>
#
# Blank lines and lines beginning with `#` are ignored.


36f0f72 .workflow/bin/queue.sh Issue #79: `reviews_waiting` converted a FAILED author lookup into an empty author set — `2>/dev/null || echo ""` — and an empty set matches no role, so a pull request was offered to every role including the one that wrote it. Both call sites now exit non-zero and say what failed. NOT YET UPSTREAMED — it belongs in agent-dev-flow and the next refresh will delete it. IT ALREADY DID: the refresh 5f8bd0d replaced this file wholesale and removed the fix, the four `internal/machinery` tests went red, `main` blocked the board, and 36f0f72 re-applied the declared hunks by hand. The sha above moved for that reason ALONE — this is the same fix, STILL local and STILL NOT UPSTREAMED, restored a second time. This entry is a debt, not a dismissal. What survives the refresh is `internal/machinery/authorlookup_test.go`, which executes the installed script against a stub `gh` that fails only the author lookup and goes red the moment the fix is gone; `queue.sh --self-test` carries a matching arm so the upstream patch arrives with its own proof. This entry is a debt, not a dismissal.
#
# IT CARRIES #59's DEBT TOO, and that is why the older line for this file is gone rather than
# forgotten. `283715b .workflow/bin/queue.sh` declared the #59 fix — `reviews_waiting` re-offering a
# head to the role that had already refused it, because a `changes-requested` verdict publishes
# `failure` and only `success` suppressed. That fix is STILL local and STILL not upstreamed — it did survive the 5f8bd0d refresh, unlike #79's, so `reviewed_by` needed no restoring; 283715b
# simply stopped being the most recent commit on the file, so the check reported the line as inert
# and it would have gone quiet either way. Both fixes leave with the same refresh, and
# `internal/machinery/reviewassignment_test.go` is what survives it for #59, exactly as
# `authorlookup_test.go` is for #79.

36f0f72 .workflow/bin/pr-authors.sh Issue #79: `--pr` printed nothing and exited 0 when the API could not be reached, so `could not determine who built this` and `determined that nobody did` shared a value. It now exits 3 — for the commit list and for the per-commit file list, where an unreadable diff was being read as a commit that touched nothing outside `openspec/`. `--range` is untouched, so `check-review.sh`, its only caller, needs no change; that was checked by driving `check-review.sh --self-test`. NOT YET UPSTREAMED — same debt, same reason, and `pr-authors.sh --self-test` carries the matching arms. Deleted by the refresh 5f8bd0d alongside its partner and restored by hand in 36f0f72; the sha moved, the debt did not.

# The healthy state is no entries at all.
#
# There was one: 17c8f9c over `.workflow/bin/check-review.sh`, the #52 fix. It was upstreamed as
# agent-dev-flow aa8c730, and the refresh 1889abd has since landed over that file — so the framework
# has spoken for it and the declaration became unnecessary while this branch was open. The check
# said so, in as many words, and the line was removed. That is the loop working.

32af7b8 .workflow/bin/gh-budget.sh Issue #81: the guard read `.resources.core.remaining`, the PRIMARY hourly quota, while every 403 in the outage was a SECONDARY (burst) limit that `GET /rate_limit` does not report — measured at core 4896/5000 and graphql 4964/5000 while every call was being refused, so the reserve was never reached and `HOLDING` never fired. It now takes the 403 itself as the signal (`note-failure`), holds on it, waits it out on the secondary's own cooldown rather than the reset clock, and says of a healthy primary quota that the secondary limit could NOT be determined. NOT YET UPSTREAMED — it belongs in agent-dev-flow and the next refresh will delete it. What survives is `internal/machinery/budgetguard_test.go`, which executes this script against a stub where the primary quota reads healthy while the calls are refused; the `--self-test` carries matching arms so the upstream patch arrives with its own proof. This entry is a debt, not a dismissal.

32af7b8 .workflow/bin/watch-queue.sh Issue #81: a poll refused by a secondary rate limit was reported as `LOOKUP FAILED` and retried on the interval, which renews the burst that caused it. It now hands the refusal to `gh-budget.sh note-failure` and emits `HOLDING` naming the burst throttle, while any other failure stays `LOOKUP FAILED` — quiet lifts a throttle and does nothing for a dial timeout. `HOLD_SLEEP` asks `hold-for` instead of `reset-in`, which was the wrong clock for the case that actually happens. NOT YET UPSTREAMED — same debt, same reason; `internal/machinery/budgetguard_test.go` drives this watch end to end and goes red without it.

32af7b8 .workflow/bin/watch-prs.sh Issue #81: the same two changes as its sibling watch, for the same reason — a refused poll becomes a named `HOLDING` rather than a `LOOKUP FAILED` retried into the throttle, and the stand-down waits on the cause's own clock. A refused `--sweep` still exits non-zero: `HOLDING` is not an answer to a question asked once. NOT YET UPSTREAMED — same debt; its `--self-test` was driven green and the sibling's Go test covers the shared guard.
